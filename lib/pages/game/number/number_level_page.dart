// 数字关卡页面
import 'package:flutter/material.dart';

class NumberLevelPage extends StatelessWidget {
  const NumberLevelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('这里是数字关卡页面'));
  }
}
